# Autogenerated with SMOP version 0.23-145-g818a6cd
# main.py --runtime=runtime --runtime=core -o stat2.py C:\Users\kevin\Documents\matlab_wrapper\matlab_wrapper\test\stat2.m
from __future__ import division
from runtime import *
from core import *
def stat2(x=None,*args,**kwargs):
    nargout = kwargs["nargout"] if kwargs else None
    varargin = cellarray(args)
    nargin = 1-[x].count(None)+len(args)

    n=length(x)
    m=avg(x,n)
    s=sqrt(sum_((x - m) ** 2 / n))
    return m,s
def avg(x=None,n=None,*args,**kwargs):
    nargout = kwargs["nargout"] if kwargs else None
    varargin = cellarray(args)
    nargin = 2-[x,n].count(None)+len(args)

    m=sum_(x) / n
    return m
